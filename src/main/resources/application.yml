eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/bms?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: hpdihpdi
jpa:
  show-sql: true
server:
  port: 8082

